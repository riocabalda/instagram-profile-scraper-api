# Local dev only (optional; falls back to 5000).
# On Render, PORT is set for you — you do not need to define PORT in Environment.
# PORT=5000

MONGO_URI=
MONGO_DB_NAME=

# Comma-separated origins for CORS (your frontend URL on Render).
FRONTEND_ORIGIN=
